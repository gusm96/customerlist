extends layout.pug 

block content 
    if errorMessage 
    span=errorMessage
    form(method="POST") 
        input(type="date", placeholder="날짜", name="date", required)
        input(type="text", placeholder="성함", name="name", required)
        input(type="text", placeholder="주소", name="address", required)
        input(type="text", placeholder="연락처", name="phNumber", required)
        input(type="text", placeholder="서비스", name="service", required)
        input(type="submit" value="작성하기")